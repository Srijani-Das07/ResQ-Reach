// Supabase client disabled - system now uses MongoDB
// This file is kept to prevent import errors but exports no functionality

console.warn('Supabase client disabled - system now uses MongoDB with offline-first approach');

export const supabase = null;